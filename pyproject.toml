[project]
name = "notes-editor"
version = "0.1.0"
description = "Daily notes editor with FastAPI and HTMX"
requires-python = ">=3.12"
dependencies = [
    "fastapi",
    "uvicorn",
    "jinja2",
    "python-multipart",
    "claude-agent-sdk",
    "python-dotenv",
]
